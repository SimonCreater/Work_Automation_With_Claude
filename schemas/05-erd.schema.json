{
  "artifact": "erd",
  "match": "05-erd",
  "required_sections": [
    "## ERD",
    "## 엔티티 명세",
    "## 관계 명세"
  ],
  "rules": [
    { "id": "no-placeholders", "type": "no_placeholders" },
    { "id": "uses-erdiagram", "type": "must_contain", "pattern": "erDiagram", "message": "ERD는 Mermaid erDiagram으로 작성합니다." },
    { "id": "has-pk", "type": "must_contain", "pattern": "PK", "message": "엔티티에 식별자(PK) 표기가 필요합니다." },
    { "id": "has-cardinality", "type": "must_contain", "pattern": "1:N|M:N|1:1", "message": "관계 명세에 카디널리티(1:1 / 1:N / M:N) 표기가 필요합니다." }
  ]
}
